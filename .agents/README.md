# Local Agent Skills

Адаптированное ядро `.agents` для `greatest-portfolio`.

Перенесено только самое полезное из `C:\Users\kri4e\Desktop\Work\ft-frontend\.agents` и подстроено под текущую целевую архитектуру:

- `Next.js` как frontend platform
- `Python` как backend platform
- `Supabase` как managed Postgres/storage/auth infrastructure
- `Playwright` как основной frontend confidence layer
- `pytest` как основной backend test layer

## Skills

- `brainstorming` - для проработки идеи до старта реализации
- `executing-plans` - для пошагового выполнения уже утверждённого плана
- `code-simplifier` - для упрощения кода без изменения поведения
- `frontend-design` - для сильного визуального и UX-направления
- `react-hook-form` - для форм и производительности RHF
- `react-next-best-practices` - для React/Next performance и архитектурной дисциплины

## Принцип использования

- сначала культивируем решение;
- потом фиксируем его в документах;
- только потом реализуем;
- не тащим в проект skill, если он завязан на чужой стек или устаревшие решения.
